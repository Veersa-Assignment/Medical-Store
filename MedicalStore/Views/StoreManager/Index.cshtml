@model StoreManager;
@if (TempData["failed"] != null)
{
    <h2>@TempData["failed"]</h2>

}
<form method="POST">
    <input asp-for="Email" />
    <input asp-for="Password" />
    <button type="submit">Login</button>

</form>